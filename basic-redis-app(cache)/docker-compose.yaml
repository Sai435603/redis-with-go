services:
  backend:
    build: .
    ports: 
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
  
  redis:
    image: redis
    container_name: go-redis
    ports:
      - "6379:6379"
